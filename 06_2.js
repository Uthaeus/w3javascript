// Write a JavaScript program to determine whether a given year is a leap year in the Gregorian calendar



const leapYear = () => {
    console.log("Enter Year (yyyy)");

    let year = prompt("Enter Year", 'default');

    return year;
}


console.log(leapYear());